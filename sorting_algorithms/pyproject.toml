[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "sorting_algorithms"
version = "1.0.0"
description = "A simple sorting library with bubble sort, merge sort and quick sort"
readme = "README.md"
requires-python = ">=3.8"

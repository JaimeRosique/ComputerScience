[build-system]
requires = ["setuptools", "setuptools-scm"]
build-backend = "setuptools.build_meta"

[project]
name = "MNIST_NN"
version = "1.0.0"
description = "A neural network of number clasification from the database MNIST"
readme = "README.md"
requires-python = ">=3.8"